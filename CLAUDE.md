# CLAUDE.md

경기도 지방재정 데이터 수집/분석. 법정 판정기준에 실제 수치를 대입해
"재정위기" 주장이 성립하는지 검증하는 것이 목적.

## 개발 환경

- Python venv: `.venv` — **PATH의 `python`은 깨져 있음**(`failed to locate pyvenv.cfg`).
  반드시 인터프리터를 직접 지정할 것: `.venv\Scripts\python.exe`
- 관련 패키지: `requests`, `python-dotenv`

```bash
.venv/Scripts/python.exe src/collect.py
```

## 데이터 소스 구조

공공데이터포털(data.go.kr)의 해당 목록들은 **API 유형이 `LINK`** 다.
data.go.kr이 API를 호스팅하지 않고 행정안전부 **지방재정365(lofin365.go.kr)** 로
연결만 해준다. 따라서 `apis.data.go.kr/...?serviceKey=&numOfRows=&pageNo=` 형태가
아니며, 실제 호출은 전부 `https://www.lofin365.go.kr/lf/hub/{코드}` 로 한다.

### 공통 기본인자 (모든 엔드포인트 동일)

| 변수명 | 타입 | 비고 |
|---|---|---|
| `Key` | STRING(필수) | 인증키. 없으면 sample key로 동작 → `pIndex=1`/`pSize=5` 고정 |
| `Type` | STRING(필수) | `xml` / `json` |
| `pIndex` | INTEGER(필수) | 페이지 위치 |
| `pSize` | INTEGER(필수) | 페이지당 건수 (기본 100) |

요청제한 횟수: 문서상 "제한없음".

### 응답코드

- `INFO-000` 정상
- `INFO-200` 해당 데이터 없음 — **오류가 아니라 결측으로 처리할 것**
- 데이터가 없으면 `<RESULT>`가 **루트 엘리먼트로** 온다.
  `root.find(".//RESULT")` 는 루트 자신을 못 잡으므로 `root.tag == "RESULT"` 를 따로 검사해야 한다.

## 엔드포인트 대응표

| 코드 | 데이터셋 | 보유연도 | 검색인자 | 산정기준 | 본청판별 |
|---|---|---|---|---|---|
| `HEDFC` | 예산대비채무비율 | 2009~2024 | `wa_laf_cd`(코드) | 예산 최종/총계/통합회계 | `laf_cd == wa_laf_cd` |
| `UNIST` | 통합재정수지비율[결산] | 2016~2024 | `wa_laf_hg_nm`(이름) | 결산/평균은 순계/통합회계 | `laf_cd` ∈ 17코드 |
| `ARBGT` | 세입예산(당초) | 2012~2026 | `wa_laf_hg_nm`(이름) | 총계/예산 당초/통합회계 | `laf_cd` ∈ 17코드 |
| `ACEXBG` | 세출예산(당초) | 2012~2026 | `wa_laf_hg_nm`(이름) | 총계/예산 당초/통합회계 | `laf_cd == wa_laf_cd` |
| `IIBBH` | 세입결산 | 2009~2024 | `wa_laf_cd`(코드) | 총계/결산 최종/통합회계 | `laf_cd == wa_laf_cd` |
| `KAAAE` | 도시군세 세목별 비중 | 2010~2024 | `fyr`만 | 순계/결산/일반회계 | 본청 개념 없음 |

수집 완료: `HEDFC`, `UNIST`. 나머지는 명세만 확인한 상태.

### 출력 필드

- `HEDFC`: `fyr`, `wa_laf_cd`, `wa_laf_hg_nm`, `laf_cd`, `laf_hg_nm`,
  `pfa_amt1`(채무잔액), `pfa_amt2`(**최종예산액**), `rate`
- `UNIST`: `fyr`, `wa_laf_hg_nm`, `laf_cd`, `laf_hg_nm`,
  `pfa_amt1`(세입), `pfa_amt2`(지출), `pfa_amt3`(순융자), `pfa_amt4`(통합재정규모), `rate`
- `ARBGT`: `fyr`, `wa_laf_hg_nm`, `laf_cd`, `laf_hg_nm`, `pfa_amt1`~`pfa_amt5`
  (세입총계/일반회계/공기업특별/기타특별/기금)
- `ACEXBG`: `fyr`, `wa_laf_cd`, `wa_laf_hg_nm`, `laf_cd`, `laf_hg_nm`,
  `ane_tott_amt`, `gen_acnt_amt`, `pbco_spc_acnt_amt`, `etc_spc_acnt_amt`, `fnd_amt`
- `IIBBH`: `fyr`, `wa_laf_cd`, `wa_laf_hg_nm`, `laf_cd`, `laf_hg_nm`,
  `total`(세입결산 총계), `pfa_amt1`~`pfa_amt4`
- `KAAAE`: `fyr`, `cap_dv_cd`/`cap_dv_nm`(시도/시/군), `dtmk_cd`/`dtmk_nm`(세목),
  `wa_laf_cd`/`wa_laf_hg_nm`(시도), `rcvmt_aggr_amt`(금액), `rate`(비중)

## 주의사항

- **필드명 체계가 3종으로 서로 다르다. 공통 파서를 쓸 수 없다.**
  `pfa_amt*` 번호 방식(HEDFC/UNIST/ARBGT/IIBBH), 의미 기반 이름(ACEXBG),
  세목 전용(KAAAE). 엔드포인트마다 파서를 따로 둘 것.
- **`KAAAE`는 순계·일반회계**라 다른 지표(총계·통합회계)와 같은 표에 놓지 말 것.
- **`KAAAE`는 시도코드가 2자리**(서울=`11`), 시도명도 전체 명칭(`서울특별시`).
  아래 7자리 코드/약칭과 매핑 테이블이 별도로 필요하다.
- **최종예산액은 `HEDFC`의 `pfa_amt2`에 있다.** 최종예산 전용 데이터셋은
  카탈로그(146종) 전체에 존재하지 않는다. 단, 이 값이 세입 기준인지 세출 기준인지는
  명세에 없으므로 `ARBGT`(세입) / `ACEXBG`(세출) 중 무엇과 짝지을지 정하고 쓸 것.
- **세종은 2012년 출범.** 2009~2011 데이터 미존재는 수집 실패가 아니라 정상이다.
  17개 시도 전수 비교 시 이 3년은 N=16이다.
- **확정 데이터는 2024년까지.** `ARBGT`/`ACEXBG`의 2025~2026은 예산 편성치이므로
  결산 확정치와 같은 층으로 취급하지 말 것.

## 17개 시도 지역코드

추측이 아니라 `wa_laf_cd` 없이 조회한 API 응답에서 distinct 추출한 값.
(세종·제주는 흔한 추측값과 다르므로 주의: 세종은 3600000이 **아니고**, 제주는 5000000이 **아니다**)

| 지역 | 코드 | 지역 | 코드 | 지역 | 코드 |
|---|---|---|---|---|---|
| 서울 | 1100000 | 세종 | 3200000 | 전북 | 4500000 |
| 부산 | 2600000 | 경기 | 4100000 | 전남 | 4600000 |
| 대구 | 2700000 | 강원 | 4200000 | 경북 | 4700000 |
| 인천 | 2800000 | 충북 | 4300000 | 경남 | 4800000 |
| 광주 | 2900000 | 충남 | 4400000 | 제주 | 4900000 |
| 대전 | 3000000 | 울산 | 3100000 | | |

본청의 `laf_cd`는 곧 해당 시도의 지역코드다. 출력에 `wa_laf_cd`가 없는
엔드포인트에서는 `laf_cd ∈ 위 17개 코드`로 본청을 판별한다
(이름이 `○○본청`으로 끝나는 행과 집합이 일치함을 확인).

## 판정 기준 (지방재정법 시행령)

시행 2026.1.2. 대통령령 제35948호

| 항목 | 조문 |
|---|---|
| 지표 정의 | 제65조제4항 |
| 재정진단 | 제65조의2제2항 |
| 재정위기·주의단체 | 제65조의3 |
| 긴급재정관리단체 | 제69조제1항 |

> 조문 번호는 사용자 제공. **임계 수치(%)는 아직 원문 대조를 하지 않았다.**
> 판정 결론을 내기 전에 조문 원문에서 기준값과 산정방식(대상회계·총계/순계)을
> 확인할 것. 특히 지표의 산정기준이 조문과 데이터셋 간에 일치하는지 봐야 한다.

## 인증키

- `.env`의 `DATA_GO_KR_KEY`에서만 읽는다. 코드 하드코딩 금지, 로그 출력 금지.
- 이 API는 lofin365 자체 발급 키다(지방재정365 OpenAPI 탭의 "인증키 신청").
  공공데이터포털 일반 인증키와 같은 체계인지는 미확인. 변수명만 관례를 따랐다.

## 호출 규칙

- API 호출은 꼭 필요할 때만. 명세 확인은 웹 문서로 하고 추측하지 말 것.
- 수집 스크립트는 **이미 저장된 원본 파일이 있으면 재호출하지 않는다**(재실행 대비).
- 응답 원문은 가공 없이 `data/raw/{엔드포인트}_{연도}_{지역코드}.xml` 로 보관
  (gitignore 처리됨).

## 산출물

| 파일 | 내용 | 행수 |
|---|---|---|
| `data/processed/budget_debt_ratio.csv` | 예산대비채무비율 2009~2024 | 3,895 |
| `data/processed/consolidated_balance_ratio.csv` | 통합재정수지비율 2016~2024 | 2,187 |

두 CSV 모두 `본청여부` 컬럼(Y/N)으로 광역 본청과 기초자치단체를 구분한다.
행은 버리지 않고 전부 보존한다.
